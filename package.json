{
  "name": "express-prisma-docker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src --legacy-watch src/index.js",
    "prod": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",

    "generate": "prisma generate",
    "migrate:prod": "prisma migrate deploy",
    "build": "npm run generate && npm run migrate:prod && npm run prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "body-parser": "^1.20.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "prisma": "^6.1.0"
  }
}
